# Server
PORT=5000
NODE_ENV=development

# MongoDB (get from MongoDB Atlas)
MONGODB_URI=mongodb+srv://doc-organizer:docorg2901@cluster0.mtp9oxp.mongodb.net/?appName=Cluster0

# JWT Secrets — make these LONG and RANDOM
JWT_SECRET=your_super_secret_jwt_key_minimum_32_chars_long
JWT_EXPIRES_IN=7d
JWT_REMEMBER_EXPIRES=30d

# Frontend URL (change in production)
CLIENT_URL=http://localhost:5173